% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_sdtmig.R
\name{validate_sdtmig}
\alias{validate_sdtmig}
\title{Validate SDTM datasets against the SDTMIG rule catalog}
\usage{
validate_sdtmig(
  datasets,
  version = "3-4",
  ct_packages = NULL,
  rule_types = NULL
)
}
\arguments{
\item{datasets}{\code{list} // \strong{required}

A named list of data frames, where each name is a domain name (e.g.,
\code{"DM"}, \code{"AE"}, \code{"LB"}) and each value is the corresponding data frame.
Names are matched case-insensitively.}

\item{version}{\verb{scalar<character>} // \emph{default:} \code{"3-4"}

The SDTMIG version string. Use hyphens (e.g., \code{"3-4"}) or dots (e.g.,
\code{"3.4"}); dots are converted to hyphens automatically. Currently only
version \code{"3-4"} is bundled.}

\item{ct_packages}{\verb{vector<character>} // \emph{default:} \code{NULL} (\code{optional})

Character vector of controlled terminology package slugs to load (e.g.,
\code{"sdtm-ct-2024-09-27"}). When \code{NULL}, the most recent bundled CT package
is loaded automatically.}

\item{rule_types}{\verb{vector<character>} // \emph{default:} \code{NULL} (\code{optional})

Optional character vector of rule types to evaluate (e.g.,
\code{"RECORD_CHECK"}). When \code{NULL}, all supported rule types are run.}
}
\value{
A \code{cdisc_conformance_result} object. Use \code{cdisc_all_passed()},
\code{cdisc_status_counts()}, \code{cdisc_issues()}, \code{cdisc_findings_df()}, and
\code{print()} to inspect the results.
}
\description{
Evaluate a collection of SDTM domain datasets against the bundled CDISC
SDTMIG conformance rule catalog. Rules are loaded from JSON files shipped
with \strong{pointblank} and evaluated directly against data frames---no external
tools, Docker containers, or API calls are needed at runtime.

The engine supports seven rule types: record checks (per-row value
validation), dataset metadata checks (column presence, variable order),
variable metadata checks, domain presence checks, dataset contents checks,
and Define-XML item/codelist checks (stubbed as always-pass until a
Define-XML importer is available in R).
}
\section{Function ID}{

13-1
}

\concept{CDISC}
