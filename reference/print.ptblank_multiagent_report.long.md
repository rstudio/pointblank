# Print the `ptblank_multiagent_report.long` object

This function will print the `ptblank_multiagent_report.long` object,
which is an HTML-based report.

## Usage

``` r
# S3 method for class 'ptblank_multiagent_report.long'
print(x, view = interactive(), ...)
```

## Arguments

- x:

  An object of class `ptblank_multiagent_report.long`.

- view:

  The value for [`print()`](https://rdrr.io/r/base/print.html)s `browse`
  argument.

- ...:

  Any additional parameters.
