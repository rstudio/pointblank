% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_yaml.R
\name{agent_yaml_write}
\alias{agent_yaml_write}
\title{Write an agent's validation plan to a YAML file}
\usage{
agent_yaml_write(agent, filename, path = NULL)
}
\arguments{
\item{agent}{An \emph{agent} object of class \code{ptblank_agent} that is created with
\code{\link[=create_agent]{create_agent()}}.}

\item{filename}{The name of the YAML file to create on disk. It is
recommended that either the \code{.yaml} or \code{.yml} extension be used for this
file.}

\item{path}{An optional path to which the YAML file should be saved (combined
with \code{filename}).}
}
\description{
With \code{agent_yaml_write()} we can take an existing \emph{agent} and write that
\emph{agent}'s validation plan to a YAML file. With that YAML, we can modify the
file if so desired, or, use it as is to create a new agent with the
\code{\link[=agent_yaml_read]{agent_yaml_read()}} function. That \emph{agent} will have a validation plan and is
ready to \code{\link[=interrogate]{interrogate()}} the data. One caveat for writing the \emph{agent} to YAML
is the condition of having a table-reading function (\code{read_fn}) set (it's a
function that is used to read the target table when \code{\link[=interrogate]{interrogate()}} is
called). This option can be set when using \code{\link[=create_agent]{create_agent()}} or with
\code{\link[=set_read_fn]{set_read_fn()}} (for use with an existing \emph{agent}).
}
