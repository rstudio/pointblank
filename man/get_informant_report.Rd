% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_metadata_report.R
\name{get_informant_report}
\alias{get_informant_report}
\title{Get a table metadata report from a metadata object}
\usage{
get_informant_report(informant)
}
\arguments{
\item{informant}{An informant object of class \code{ptblank_informant}.}
}
\value{
A \strong{gt} table object.
}
\description{
We can get a table metadata report from a metadata object that's generated by
the \code{\link[=create_metadata]{create_metadata()}} function. The report is provided as a \strong{gt} based
display table. The amount of metadata shown depends on the extent of that
added via the use of the \verb{meta_*()} functions or addition of metadata through
direct editing of a \strong{pointblank} YAML file.
}
\examples{
# Generate a metadata object using
# the `small_table` dataset
metadata <- create_metadata(small_table)

# This function creates some metadata
# without any extra help by profiling
# the supplied table object; it adds
# the sections: (1) 'table' and
# (2) 'columns' and we can print the
# object to see the metadata report
# metadata

# Alternatively, we can visualize this
# metadata into the same report by using
# `get_informant_report()`
report <- get_informant_report(metadata)
class(report)

}
